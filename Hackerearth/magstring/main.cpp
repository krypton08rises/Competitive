#include <iostream>
#include <string>
#include <vector>

using namespace std;

int main()
{
        string s;
        int q,i;
        vector<int> n,res;
        getline(cin,string);
        cin>>q;
        for(i=0;i<q;i++)
        {
            n.push_back(0);
            res.push_back(0);
            cin>>n[i];
        }
        for(i=0;i<q;i++)
}
